// programm to determine if a number is even or odd
#include <stdio.h>
int (){
    int num;
    printf("enter your number:");
    scanf("%d",&num);
    if(num % 2==0)
    printf("The number is even",num);
    else
printf("The number is odd",num);
return 0;
}
