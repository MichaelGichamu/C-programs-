// programm to determine if a number is divisible by 5
#include <stdio.h>
int main(){
    int num;
    printf("enter your number:");
    scanf("%d",&num);
    if(num % 5==0)
    printf("The number is divisble by 5",num);
    else
printf("The number is not by divisble by 5",num);
return 0;
}
